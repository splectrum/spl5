#!/usr/bin/env bash
# spl-server — Splectrum RPC server.
# System service: serves requests for any repo on the filesystem.
DIR="$(cd "$(dirname "$0")/.." && pwd)"
exec bare "$DIR/mycelium/server.js" "$@"
